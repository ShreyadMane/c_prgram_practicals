#include<stdio.h>
int main(){
    int a,b;
    printf("Enter the values=");
    scanf("%d",&a,&b);
    if(a<b){
        printf("A is less than b");
    }else{
    printf("A is greater than b");
    }
}
